server:
  port: 9655
spring:
  servlet:
    multipart:
      max-file-size: 10MB # 设置单个文件的最大上传大小
      max-request-size: 10MB # 设置整个请求的最大上传大小
  main:
    allow-circular-references: true
  datasource:
    url: jdbc:mysql://localhost:3306/demo5
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:mapper/*.xml
files:
  upload:
    path: C:\Users\合格的牛马\Desktop\demo5\file\
